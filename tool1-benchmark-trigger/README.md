# Tool 1 – Automated Benchmark Trigger

## Purpose
Automates repeated in-game benchmark execution and triggers CapFrameX to start FPS capture at each run.

## Workflow
1. Tool launchs game benchmark
2. Tool triggers CapFrameX capture
3. Benchmark completes
4. CapFrameX generates FPS JSON output

## Notes
- Requires CapFrameX to be installed and configured
- Currently supports Marvel Rivals

